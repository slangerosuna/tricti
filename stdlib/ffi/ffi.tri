# Legacy shim: the FFI implementation now lives in `stdlib/ffi.tri` so that it can
# be imported as `use ffi` or `use std::ffi`. This file remains as a placeholder to
# avoid breaking older paths that referenced `ffi::mod` directly.
