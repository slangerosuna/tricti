# IO namespace aggregator for the TriCTI stdlib.
# Provides convenient aliased access to the POSIX-backed IO surface.

use std::io::dir
use std::io::file
use std::io::metadata
use std::io::path
use std::io::posix
